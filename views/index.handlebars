

{{#each posts}}
    <div class="post">
        <h2>{{title}}</h2>
        <p>{{post_content}}</p>
        <p class="author">By {{user.userName}}</p>
        <a href="/post/{{id}}">Edit</a>
        <a href="/post/{{id}}">Delete</a>
    </div>
{{/each}}